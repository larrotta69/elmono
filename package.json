{
  "name": "elmono",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "lint:all": "yarn workspaces lint",
    "clean": "rimraf node_modules tools/eslint-config/node_modules && yarn",
    "build:ui": "yarn workspace @elmono/ui build"
  },
  "workspaces": [
    "packages/*",
    "tools/*"
  ]
}
