{
  "name": "elmono",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "lint:all": "yarn workspaces run lint",
    "clean": "rimraf node_modules tools/eslint-config/node_modules && yarn",
    "ui:build": "yarn workspace @elmono/ui build",
    "storybook": "yarn workspace @elmono/storybook storybook"
  },
  "workspaces": [
    "packages/*",
    "tools/*"
  ]
}
